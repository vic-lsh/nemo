#ifndef NEMO_LIBS_UTIL_H
#define NEMO_LIBS_UTIL_H

#include <stdint.h>

#define KB(x) (((uint64_t)x) * 1024)
#define MB(x) (KB(x) * 1024)
#define GB(x) (MB(x) * 1024)
#define TB(x) (GB(x) * 1024)

#endif /* NEMO_LIBS_UTIL_H */
